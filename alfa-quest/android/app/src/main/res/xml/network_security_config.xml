<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Release: only system CAs, no cleartext -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>

    <!-- Debug: trust user-installed CAs too (for Charles Proxy, etc) -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system"/>
            <certificates src="user"/>
        </trust-anchors>
    </debug-overrides>

    <!-- Allow cleartext only to Android emulator loopback -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>

    <!-- Trust domains explicitly -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">operacao-alfa.mydevhomolog.live</domain>
        <domain includeSubdomains="true">operacaoalfa.com.br</domain>
        <trust-anchors>
            <certificates src="system"/>
            <certificates src="user"/>
        </trust-anchors>
    </domain-config>
</network-security-config>
